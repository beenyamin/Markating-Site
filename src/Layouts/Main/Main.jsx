import { Outlet } from "react-router-dom";

const Main = () => {
      return (
            <div className="font-Urbanist">
                
                 <Outlet></Outlet>
            </div>
      );
};

export default Main;